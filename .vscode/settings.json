{
  "kiroAgent.configureMCP": "Enabled",
  "kiroAgent.trustedCommands": [
    "pnpm *",
    "Start-Job *",
    "ren *",
    "Rename-Item *",
    "npx *",
    "node *",
    "turbo *",
    "copy *",
    "turbo run build lint type-check --dry=json | jq '.tasks | length'",
    "$result *",
    "echo *",
    "npm *",
    "timeout *",
    "Start-Job -ScriptBlock { pnpm run dev } | Wait-Job -Timeout 10 | Receive-Job"
  ]
}
